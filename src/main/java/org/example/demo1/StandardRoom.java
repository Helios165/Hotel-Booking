package org.example.demo1;
/*
public class StandardRoom extends Room{

    private String bedType;
    private boolean hasTV;

    StandardRoom(long ID, float price, boolean booked, Date availableDate, String hotelName, String description, String bedType, boolean hasTV) {
        super(ID, price, booked, availableDate, hotelName, description);
        this.bedType = bedType;
        this.hasTV = hasTV;
    }
    String getBedType() {
        return bedType;
    }
    void setBedType(String bedType) {
        this.bedType = bedType;
    }
    boolean hasTV() {
        return hasTV;
    }
    void setHasTV(boolean hasTV) {
        this.hasTV = hasTV;
    }
    @Override
    public boolean equals(Object obj) 
    {
        if (this == obj) return true;
        if (!(obj instanceof StandardRoom)) return false;
        if (!super.equals(obj)) return false;
        StandardRoom that = (StandardRoom) obj;
        return hasTV == that.hasTV && bedType.equals(that.bedType);
    }

    
}

 */